DATABASE_URL=postgresql+asyncpg://dadas:dadas_dev@localhost:5432/dadaskuyumculuk

# api.finansveri.com X-API-Key
FINANSVERI_API_KEY=replace_me
FINANSVERI_BASE_URL=https://api.finansveri.com

# JWT (en az 32 karakter)
JWT_SECRET=replace_with_long_random_string_at_least_32_chars
JWT_EXPIRES_HOURS=168

# Bootstrap admin (ilk migration'da yaratılır)
ADMIN_BOOTSTRAP_USERNAME=admin
ADMIN_BOOTSTRAP_PASSWORD=admin123

# Worker poll aralığı (saniye)
POLL_INTERVAL_SECONDS=1

# CORS
CORS_ORIGINS=http://localhost:3000

ENV=dev
